name: Setup NodeJS
on: workflow_dispatch
jobs:
  nodejs-job:
    runs-on:
      - ubuntu-latest
    steps:
      - name: nodejs-job
        uses: actions/setup-node@v6.5.0
        with:
           node-version: 22
           
      - name: check npm version
        run: npm version
    
